# Visual identity

Currently the frontend uses a very basic default Primevue colour scheme and basically doesn't touch the components' looks at all. This results in a very generic looking application right now, without any identity or style of its own. 
We should work on a more refined look for the application. 